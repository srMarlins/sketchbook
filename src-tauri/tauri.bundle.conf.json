{
  "$schema": "https://schema.tauri.app/config/2",
  "_comment": "Production-bundle-only overlay. Merged into tauri.conf.json by `tauri build --config tauri.bundle.conf.json`. Keeps the python tree out of the dev config so tauri-build doesn't traverse thousands of bundled-python files on every cargo rebuild.",
  "bundle": {
    "resources": {
      "binaries/python": "python",
      "binaries/dist": "dist"
    }
  }
}
