<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ArgumentListWrapping:JournalAction.kt:(", ")</ID>
    <ID>BlankLineBetweenWhenConditions:JournalAction.kt:when (action) { is ActionRecord.Move -&gt; "Moved ${filenameOf(action.pathAfter)} — ${parentDirOf(action.pathBefore)}/ → ${parentDirOf(action.pathAfter)}/" is ActionRecord.Rename -&gt; "Renamed ${action.nameBefore} → ${action.nameAfter}" is ActionRecord.Archive -&gt; if (action.isArchived) "Archived" else "Unarchived" is ActionRecord.SetTags -&gt; "Tags: ${action.before.joinToString(", ").ifEmpty { "(none)" }} → ${action.after.joinToString(", ").ifEmpty { "(none)" }}" is ActionRecord.ForceTakeLock -&gt; "Force-took lock from ${action.priorOwnerHostName ?: "unknown host"}" is ActionRecord.PushConflict -&gt; "Push conflict (ours rev ${action.ourRev} vs theirs rev ${action.theirRev})" is ActionRecord.MissingSampleMapped -&gt; "Relink ${filenameOf(action.missingPath)} → ${parentDirOf(action.candidatePath)}/" + alsOutcomeSuffix(action.alsOutcome) is ActionRecord.MissingSampleUnmapped -&gt; "Undo relink ${filenameOf(action.missingPath)}" + alsOutcomeSuffix(action.alsOutcome) is ActionRecord.MacPathRepaired -&gt; "Repair Mac paths (${action.mappingCount})" + alsOutcomeSuffix(action.alsOutcome) is ActionRecord.MacPathRestored -&gt; "Undo Mac-path repair (${action.mappingCount})" + alsOutcomeSuffix(action.alsOutcome) is ActionRecord.SnapshotRelabeled -&gt; { val before = action.labelBefore?.takeIf { it.isNotBlank() } ?: "(unlabeled)" val after = action.labelAfter?.takeIf { it.isNotBlank() } ?: "(unlabeled)" "Relabel snapshot $before → $after" } is ActionRecord.StageOverridden -&gt; "Stage override ${action.stageBefore ?: "(auto)"} → ${action.stageAfter ?: "(auto)"}" }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalAction.kt:when (action) { is ActionRecord.Move -&gt; JournalLabel( verb = "Move", target = filenameOf(action.pathAfter), detail = "${parentDirOf(action.pathBefore)}/ → ${parentDirOf(action.pathAfter)}/", tintHint = VerbTint.Action, ) is ActionRecord.Rename -&gt; JournalLabel( verb = "Rename", target = action.nameAfter, detail = "← ${action.nameBefore}", tintHint = VerbTint.Action, ) is ActionRecord.Archive -&gt; JournalLabel( verb = if (action.isArchived) "Archive" else "Unarchive", target = "", tintHint = if (action.isArchived) VerbTint.Remove else VerbTint.Add, ) is ActionRecord.SetTags -&gt; JournalLabel( verb = "Tag", target = action.after.joinToString(", ").ifEmpty { "(none)" }, detail = "← ${action.before.joinToString(", ").ifEmpty { "(none)" }}", tintHint = if (action.after.isEmpty()) VerbTint.Remove else VerbTint.Add, ) is ActionRecord.ForceTakeLock -&gt; JournalLabel( verb = "Lock", target = "force-take", detail = action.priorOwnerHostName?.let { "from $it" }, tintHint = VerbTint.Neutral, ) is ActionRecord.PushConflict -&gt; JournalLabel( verb = "Conflict", target = "push diverged", detail = "ours rev ${action.ourRev} vs theirs rev ${action.theirRev}", tintHint = VerbTint.Remove, ) is ActionRecord.MissingSampleMapped -&gt; JournalLabel( verb = "Relink", target = filenameOf(action.missingPath), detail = "→ ${parentDirOf(action.candidatePath)}/${alsOutcomeSuffix(action.alsOutcome)}", tintHint = VerbTint.Repair, ) is ActionRecord.MissingSampleUnmapped -&gt; JournalLabel( verb = "Undo relink", target = filenameOf(action.missingPath), detail = alsOutcomeSuffix(action.alsOutcome).trim().ifEmpty { null }, tintHint = VerbTint.Repair, ) is ActionRecord.MacPathRepaired -&gt; JournalLabel( verb = "Repair", target = "${action.mappingCount} mac paths", detail = alsOutcomeSuffix(action.alsOutcome).trim().ifEmpty { null }, tintHint = VerbTint.Repair, ) is ActionRecord.MacPathRestored -&gt; JournalLabel( verb = "Undo repair", target = "${action.mappingCount} mac paths", detail = alsOutcomeSuffix(action.alsOutcome).trim().ifEmpty { null }, tintHint = VerbTint.Repair, ) is ActionRecord.SnapshotRelabeled -&gt; { val before = action.labelBefore?.takeIf { it.isNotBlank() } ?: "(unlabeled)" val after = action.labelAfter?.takeIf { it.isNotBlank() } ?: "(unlabeled)" JournalLabel( verb = "Relabel", target = after, detail = "← $before", tintHint = VerbTint.Action, ) } is ActionRecord.StageOverridden -&gt; JournalLabel( verb = "Stage", target = action.stageAfter ?: "(auto)", detail = "← ${action.stageBefore ?: "(auto)"}", tintHint = VerbTint.Action, ) }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalAction.kt:when (outcome) { "Patched", "NoChange", "" -&gt; "" // happy path or "no patch needed" — both are silent "SkippedBusy" -&gt; " (.als open in Live — skipped)" "Failed" -&gt; " (.als write failed)" "NoUndoBytes" -&gt; " (no pre-patch snapshot — catalog only)" else -&gt; " ($outcome)" }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalDetailPane.kt:when (action) { is ActionRecord.Move -&gt; listOf( "From" to action.pathBefore, "To" to action.pathAfter, ) is ActionRecord.Rename -&gt; listOf( "From" to action.nameBefore, "To" to action.nameAfter, ) is ActionRecord.Archive -&gt; listOf( "Was archived" to action.wasArchived.toString(), "Is archived" to action.isArchived.toString(), ) is ActionRecord.SetTags -&gt; listOf( "Before" to action.before.joinToString(", ").ifEmpty { "(none)" }, "After" to action.after.joinToString(", ").ifEmpty { "(none)" }, ) is ActionRecord.ForceTakeLock -&gt; listOf( "Prior owner" to (action.priorOwnerHostName ?: "(unknown)"), ) is ActionRecord.PushConflict -&gt; listOf( "Our rev" to action.ourRev.toString(), "Their rev" to action.theirRev.toString(), ) is ActionRecord.MissingSampleMapped -&gt; listOf( "Missing" to action.missingPath, "Candidate" to action.candidatePath, ".als outcome" to action.alsOutcome, ) is ActionRecord.MissingSampleUnmapped -&gt; listOf( "Missing" to action.missingPath, "Candidate" to action.candidatePath, ".als outcome" to action.alsOutcome, ) is ActionRecord.MacPathRepaired -&gt; listOf( "Mappings" to action.mappingCount.toString(), ".als outcome" to action.alsOutcome, ) is ActionRecord.MacPathRestored -&gt; listOf( "Mappings" to action.mappingCount.toString(), ".als outcome" to action.alsOutcome, ) is ActionRecord.SnapshotRelabeled -&gt; listOf( "Rev" to action.rev.toString(), "Before" to (action.labelBefore.takeUnless { it.isNullOrBlank() } ?: "(unlabeled)"), "After" to (action.labelAfter.takeUnless { it.isNullOrBlank() } ?: "(unlabeled)"), ) is ActionRecord.StageOverridden -&gt; listOf( "Inferred" to (action.stageInferred ?: "(none)"), "Before" to (action.stageBefore ?: "(auto)"), "After" to (action.stageAfter ?: "(auto)"), ) }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalDetailPane.kt:when (action) { is ActionRecord.Move, is ActionRecord.Rename, is ActionRecord.Archive, is ActionRecord.SetTags -&gt; true else -&gt; false }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:JournalViewModel$when (intent) { is Intent.OpenProject -&gt; _effects.tryEmit(Effect.NavigateToProject(intent.projectId)) is Intent.SetActionTypeFilter -&gt; filters.update { it.copy(actionType = intent.filter) } is Intent.SetSearch -&gt; filters.update { it.copy(search = intent.query) } is Intent.SetDateRange -&gt; filters.update { it.copy(dateRange = intent.range) } is Intent.UndoOne -&gt; viewModelScope.launch { val r = undoOne(intent.entry) if (r.isSuccess) _effects.tryEmit(Effect.Undone(intent.entry)) else _effects.tryEmit( Effect.UndoFailed(intent.entry, r.exceptionOrNull()?.message ?: "unknown"), ) } is Intent.BulkUndo -&gt; viewModelScope.launch { val successes = mutableListOf&lt;JournalEntry&gt;() val failures = mutableListOf&lt;Pair&lt;JournalEntry, String&gt;&gt;() for (e in intent.entries) { val r = undoOne(e) if (r.isSuccess) successes += e else failures += e to (r.exceptionOrNull()?.message ?: "unknown") } _effects.tryEmit(Effect.BulkUndone(successes, failures)) } }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:JournalViewModel$when (val a = entry.action) { is ActionRecord.Move -&gt; "${a.pathBefore} ${a.pathAfter}" is ActionRecord.Rename -&gt; "${a.nameBefore} ${a.nameAfter}" is ActionRecord.SetTags -&gt; "${a.before.joinToString(" ")} ${a.after.joinToString(" ")}" is ActionRecord.ForceTakeLock -&gt; a.priorOwnerHostName.orEmpty() is ActionRecord.MissingSampleMapped -&gt; "${a.missingPath} ${a.candidatePath}" is ActionRecord.MissingSampleUnmapped -&gt; "${a.missingPath} ${a.candidatePath}" is ActionRecord.SnapshotRelabeled -&gt; "${a.labelBefore.orEmpty()} ${a.labelAfter.orEmpty()}" is ActionRecord.StageOverridden -&gt; "${a.stageBefore.orEmpty()} ${a.stageAfter.orEmpty()}" is ActionRecord.Archive, is ActionRecord.PushConflict, is ActionRecord.MacPathRepaired, is ActionRecord.MacPathRestored -&gt; "" }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:JournalViewModel$when (val a = entry.action) { is ActionRecord.Move -&gt; { val current = projects.observeProject(entry.projectId).first() when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.path.value != a.pathAfter -&gt; Result.failure( IllegalStateException("file is no longer at the recorded location — undo skipped"), ) else -&gt; projects.move(entry.projectId, parentDirOf(a.pathBefore)).map {} } } is ActionRecord.Rename -&gt; { val current = projects.observeProject(entry.projectId).first() when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.name != a.nameAfter -&gt; Result.failure( IllegalStateException("project no longer named ${a.nameAfter} — undo skipped"), ) else -&gt; projects.rename(entry.projectId, a.nameBefore).map {} } } is ActionRecord.Archive -&gt; projects.archive(entry.projectId, a.wasArchived).map {} is ActionRecord.SetTags -&gt; projects.setTags(entry.projectId, a.before).map {} is ActionRecord.MacPathRepaired -&gt; repair.restoreMacPathRepair(entry.projectId) else -&gt; Result.failure(IllegalArgumentException("not invertible")) }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:JournalViewModel$when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.name != a.nameAfter -&gt; Result.failure( IllegalStateException("project no longer named ${a.nameAfter} — undo skipped"), ) else -&gt; projects.rename(entry.projectId, a.nameBefore).map {} }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:JournalViewModel$when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.path.value != a.pathAfter -&gt; Result.failure( IllegalStateException("file is no longer at the recorded location — undo skipped"), ) else -&gt; projects.move(entry.projectId, parentDirOf(a.pathBefore)).map {} }</ID>
    <ID>BlankLineBetweenWhenConditions:JournalViewModel.kt:when (entry.action) { is ActionRecord.Move, is ActionRecord.Rename, is ActionRecord.Archive, is ActionRecord.SetTags, is ActionRecord.MacPathRepaired -&gt; true else -&gt; false }</ID>
    <ID>ClassSignature:JournalViewModel.kt:JournalViewModel$DayGroup</ID>
    <ID>ClassSignature:JournalViewModel.kt:JournalViewModel$JournalRow</ID>
    <ID>ClassSignature:JournalViewModel.kt:JournalViewModel.Effect$BulkUndone : Effect</ID>
    <ID>CyclomaticComplexMethod:JournalAction.kt:fun journalLabel: JournalLabel</ID>
    <ID>FunctionExpressionBody:JournalViewModel.kt:JournalViewModel${ return when (val a = entry.action) { is ActionRecord.Move -&gt; { val current = projects.observeProject(entry.projectId).first() when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.path.value != a.pathAfter -&gt; Result.failure( IllegalStateException("file is no longer at the recorded location — undo skipped"), ) else -&gt; projects.move(entry.projectId, parentDirOf(a.pathBefore)).map {} } } is ActionRecord.Rename -&gt; { val current = projects.observeProject(entry.projectId).first() when { current == null -&gt; Result.failure(IllegalStateException("project not found")) current.name != a.nameAfter -&gt; Result.failure( IllegalStateException("project no longer named ${a.nameAfter} — undo skipped"), ) else -&gt; projects.rename(entry.projectId, a.nameBefore).map {} } } is ActionRecord.Archive -&gt; projects.archive(entry.projectId, a.wasArchived).map {} is ActionRecord.SetTags -&gt; projects.setTags(entry.projectId, a.before).map {} is ActionRecord.MacPathRepaired -&gt; repair.restoreMacPathRepair(entry.projectId) else -&gt; Result.failure(IllegalArgumentException("not invertible")) } }</ID>
    <ID>FunctionLiteral:JournalAction.kt:&lt;anonymous&gt;</ID>
    <ID>FunctionNaming:JournalDetailPane.kt:@Composable fun JournalDetailPane</ID>
    <ID>FunctionNaming:JournalDetailPane.kt:@Composable private fun LabelledRow</ID>
    <ID>FunctionNaming:JournalScreen.kt:@Composable fun JournalFilterBar</ID>
    <ID>FunctionNaming:JournalScreen.kt:@Composable private fun DayGroupCard</ID>
    <ID>FunctionNaming:JournalScreen.kt:@Composable private fun JournalRowItem</ID>
    <ID>FunctionNaming:JournalScreen.kt:@Composable private fun UndoButton</ID>
    <ID>FunctionSignature:JournalAction.kt:private fun filenameOf: String</ID>
    <ID>FunctionSignature:JournalDetailPane.kt:@Composable fun JournalDetailPane</ID>
    <ID>Kdoc:JournalScreen.kt:/** * Journal viewer with filters, search, and day grouping. Bulk-undo lives on the first day's card * header when filters are active and at least one row is invertible — most-narrow-result wins, * since users typically narrow first ("show me yesterday's renames") and then bulk-undo. * * `detailPane` is an optional slot the host wires (RootContent owns navigation). When null, row * clicks are no-ops and the screen renders read-only. * * Performance: each day card and the rows within it stay as a single LazyColumn item so * journal-volume (capped at 200 rows) stays cheap. Per-row composables take a remembered * @Immutable view so Compose skips unchanged rows on parent recomposition. */</ID>
    <ID>MagicNumber:JournalScreen.kt:6</ID>
    <ID>MagicNumber:JournalViewModel.kt:JournalViewModel$10</ID>
    <ID>MagicNumber:JournalViewModel.kt:JournalViewModel$6</ID>
    <ID>MatchingDeclarationName:JournalAction.kt:JournalLabel</ID>
    <ID>ModifierMissing:JournalDetailPane.kt:@Composable fun JournalDetailPane</ID>
    <ID>MultiLineIfElse:JournalScreen.kt:"Loading…"</ID>
    <ID>MultiLineIfElse:JournalScreen.kt:"Move/rename/archive/tag actions land here."</ID>
    <ID>MultiLineIfElse:JournalScreen.kt:"No entries"</ID>
    <ID>MultiLineIfElse:JournalScreen.kt:"No matches"</ID>
    <ID>MultiLineIfElse:JournalScreen.kt:"Nothing matches the current filters. Clear them to see everything."</ID>
    <ID>MultiLineIfElse:JournalViewModel.kt:JournalViewModel$_effects.tryEmit( Effect.UndoFailed(intent.entry, r.exceptionOrNull()?.message ?: "unknown"), )</ID>
    <ID>MultiLineIfElse:JournalViewModel.kt:JournalViewModel$_effects.tryEmit(Effect.Undone(intent.entry))</ID>
    <ID>MultiLineIfElse:JournalViewModel.kt:JournalViewModel$failures += e to (r.exceptionOrNull()?.message ?: "unknown")</ID>
    <ID>MultiLineIfElse:JournalViewModel.kt:JournalViewModel$successes += e</ID>
    <ID>TrailingCommaOnDeclarationSite:JournalDetailPane.kt:is ActionRecord.Move, is ActionRecord.Rename, is ActionRecord.Archive, is ActionRecord.SetTags -&gt; true</ID>
    <ID>TrailingCommaOnDeclarationSite:JournalViewModel.kt:JournalViewModel$is ActionRecord.Archive, is ActionRecord.PushConflict, is ActionRecord.MacPathRepaired, is ActionRecord.MacPathRestored -&gt; ""</ID>
    <ID>TrailingCommaOnDeclarationSite:JournalViewModel.kt:is ActionRecord.Move, is ActionRecord.Rename, is ActionRecord.Archive, is ActionRecord.SetTags, is ActionRecord.MacPathRepaired -&gt; true</ID>
  </CurrentIssues>
</SmellBaseline>
