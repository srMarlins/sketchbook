<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>BlankLineBetweenWhenConditions:NeedsAttentionScreen.kt:when (kind) { MissingKind.Auto -&gt; Badge(color = AppTheme.colors.tintSage) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text( "→ ${autoMatchParent.ifEmpty { "match" }}/", style = AppTheme.typography.caption, maxLines = 1, ) } } MissingKind.Multi -&gt; Badge(color = AppTheme.colors.tintBlue) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text("$candidatesCount matches", style = AppTheme.typography.caption) } } MissingKind.None -&gt; Badge(color = AppTheme.colors.tintRose) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text("no match", style = AppTheme.typography.caption) } } }</ID>
    <ID>BlankLineBetweenWhenConditions:NeedsAttentionScreen.kt:when (section.kind) { MissingKind.Auto -&gt; Button( onClick = { onBulkApply(findings) }, variant = ButtonVariant.Primary, ) { Text("Apply ${section.entries.size}", softWrap = false, maxLines = 1) } MissingKind.None -&gt; Button( onClick = { onBulkDismiss(findings) }, variant = ButtonVariant.Ghost, ) { Text("Dismiss ${section.entries.size}", softWrap = false, maxLines = 1) } MissingKind.Multi -&gt; Unit }</ID>
    <ID>BlankLineBetweenWhenConditions:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$when (intent) { is Intent.SetSearch -&gt; search.value = intent.query is Intent.AckMacImport -&gt; viewModelScope.launch { val r = repository.acknowledgeMacImport(intent.projectId) emitAck(r.isSuccess, intent.projectId.value.toString(), "ack") } is Intent.RepairMacPaths -&gt; viewModelScope.launch { pending.update { it.copy(macRepairs = it.macRepairs + intent.projectId) } val r = runCatching { repository.applyMacPathRepair(intent.projectId) } .getOrElse { Result.failure(it) } if (r.isSuccess) { // Auto-cleanup will drop pending once the row leaves macImports. _effects.tryEmit(Effect.MatchApplied(intent.projectId.value.toString())) } else { pending.update { it.copy(macRepairs = it.macRepairs - intent.projectId) } _effects.tryEmit(Effect.Failed(intent.projectId.value.toString(), "repair failed")) } } is Intent.DismissMissingSample -&gt; viewModelScope.launch { val r = repository.dismissMissingSample(intent.projectId, intent.missingPath) emitAck(r.isSuccess, intent.projectId.value.toString(), "dismiss") } is Intent.ApplyMatch -&gt; viewModelScope.launch { val key = intent.projectId to intent.missingPath pending.update { it.copy(missingApplies = it.missingApplies + key) } val r = runCatching { repository.applyMissingSampleMatch( projectId = intent.projectId, missingPath = intent.missingPath, candidatePath = intent.candidatePath, ) }.getOrElse { Result.failure(it) } if (r.isSuccess) { _effects.tryEmit(Effect.MatchApplied(intent.projectId.value.toString())) } else { pending.update { it.copy(missingApplies = it.missingApplies - key) } _effects.tryEmit(Effect.Failed(intent.projectId.value.toString(), "apply failed")) } } is Intent.BulkAck -&gt; viewModelScope.launch { val successes = mutableListOf&lt;String&gt;() val failures = mutableListOf&lt;String&gt;() for (id in intent.projectIds) { val r = runCatching { repository.acknowledgeMacImport(id) } .getOrElse { Result.failure(it) } val k = id.value.toString() if (r.isSuccess) successes += k else failures += k } _effects.tryEmit(Effect.BulkAcked(successes, failures)) } is Intent.BulkRepairMacPaths -&gt; viewModelScope.launch { pending.update { it.copy(macRepairs = it.macRepairs + intent.projectIds) } val successes = mutableListOf&lt;String&gt;() val failures = mutableListOf&lt;String&gt;() for (id in intent.projectIds) { val r = runCatching { repository.applyMacPathRepair(id) } .getOrElse { Result.failure(it) } val k = id.value.toString() if (r.isSuccess) { successes += k } else { failures += k pending.update { it.copy(macRepairs = it.macRepairs - id) } } } _effects.tryEmit(Effect.BulkRepaired(successes, failures)) } is Intent.BulkApplyAutoMatch -&gt; viewModelScope.launch { val keys = intent.findings.map { it.projectId to it.missingPath } pending.update { it.copy(missingApplies = it.missingApplies + keys) } val successes = mutableListOf&lt;String&gt;() val failures = mutableListOf&lt;String&gt;() for (f in intent.findings) { val key = f.projectId to f.missingPath val resultKey = "${f.projectId.value}|${f.missingPath}" val auto = f.autoMatch if (auto == null) { failures += resultKey pending.update { it.copy(missingApplies = it.missingApplies - key) } continue } val r = runCatching { repository.applyMissingSampleMatch(f.projectId, f.missingPath, auto.path) }.getOrElse { Result.failure(it) } if (r.isSuccess) { successes += resultKey } else { failures += resultKey pending.update { it.copy(missingApplies = it.missingApplies - key) } } } _effects.tryEmit(Effect.BulkApplied(successes, failures)) } is Intent.BulkDismiss -&gt; viewModelScope.launch { val successes = mutableListOf&lt;String&gt;() val failures = mutableListOf&lt;String&gt;() for (f in intent.findings) { val r = runCatching { repository.dismissMissingSample(f.projectId, f.missingPath) }.getOrElse { Result.failure(it) } val k = "${f.projectId.value}|${f.missingPath}" if (r.isSuccess) successes += k else failures += k } _effects.tryEmit(Effect.BulkDismissed(successes, failures)) } }</ID>
    <ID>ClassSignature:NeedsAttentionViewModel.kt:NeedsAttentionViewModel : ViewModel</ID>
    <ID>ClassSignature:NeedsAttentionViewModel.kt:NeedsAttentionViewModel.Intent$ApplyMatch : Intent</ID>
    <ID>CyclomaticComplexMethod:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$fun dispatch</ID>
    <ID>FunctionNaming:NeedsAttentionDetailPane.kt:@Composable fun NeedsAttentionDetailPane</ID>
    <ID>FunctionNaming:NeedsAttentionDetailPane.kt:@Composable private fun LabelledRow</ID>
    <ID>FunctionNaming:NeedsAttentionDetailPane.kt:@Composable private fun MacImportDetail</ID>
    <ID>FunctionNaming:NeedsAttentionDetailPane.kt:@Composable private fun MissingSampleDetail</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable fun NeedsAttentionFilterBar</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable private fun IconAction</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable private fun MacImportCard</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable private fun MacImportRow</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable private fun MissingSampleRow</ID>
    <ID>FunctionNaming:NeedsAttentionScreen.kt:@Composable private fun MissingSamplesCard</ID>
    <ID>FunctionSignature:NeedsAttentionDetailPane.kt:@Composable fun NeedsAttentionDetailPane</ID>
    <ID>FunctionSignature:NeedsAttentionDetailPane.kt:@Composable private fun MacImportDetail</ID>
    <ID>FunctionSignature:NeedsAttentionDetailPane.kt:@Composable private fun MissingSampleDetail</ID>
    <ID>FunctionSignature:NeedsAttentionScreen.kt:private fun filenameOf: String</ID>
    <ID>Kdoc:NeedsAttentionScreen.kt:/** * Repair surface. Two top-level [GroupCard]s — Mac-imported and Missing samples — visually * matching the Proposals queue: rounded-corner card with a tintCream header band, bulk action in * the header, and flat hairline-divided rows nested as the body. Mac variations cluster (sorted * by stripped base name); missing samples sub-group by confidence (Auto / Multi / None) using * [SubGroupHeader] inside the same card. * * Performance: each card is one LazyColumn item, rows inside use [key] blocks so Compose can * skip unchanged rows when the pending set churns during a bulk repair. Per-row composables take * primitive/`@Immutable` args — no full finding-object dependency at the row argument level. * * `detailPane` is an optional slot the host wires (RootContent owns navigation). */</ID>
    <ID>LongMethod:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$fun dispatch</ID>
    <ID>MatchingDeclarationName:NeedsAttentionDetailPane.kt:NeedsAttentionDetailTarget</ID>
    <ID>MaxLineLength:NeedsAttentionDetailPane.kt:"Repair rewrites the .als so Live finds samples on this machine. Ignore drops the project from this list without touching it."</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:"All clear"</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:"No Mac-imported projects or missing samples found."</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:"No matches"</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:"Nothing matches \"${state.search}\". Clear the search to see everything."</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:"Scanning…"</ID>
    <ID>MultiLineIfElse:NeedsAttentionScreen.kt:when (kind) { MissingKind.Auto -&gt; Badge(color = AppTheme.colors.tintSage) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text( "→ ${autoMatchParent.ifEmpty { "match" }}/", style = AppTheme.typography.caption, maxLines = 1, ) } } MissingKind.Multi -&gt; Badge(color = AppTheme.colors.tintBlue) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text("$candidatesCount matches", style = AppTheme.typography.caption) } } MissingKind.None -&gt; Badge(color = AppTheme.colors.tintRose) { ProvideContentColor(AppTheme.colors.inkPrimary) { Text("no match", style = AppTheme.typography.caption) } } }</ID>
    <ID>MultiLineIfElse:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$findings.macImports</ID>
    <ID>MultiLineIfElse:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$findings.macImports.filter { it.matchesSearch(q) }</ID>
    <ID>MultiLineIfElse:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$findings.missingSamples</ID>
    <ID>MultiLineIfElse:NeedsAttentionViewModel.kt:NeedsAttentionViewModel$findings.missingSamples.filter { it.matchesSearch(q) }</ID>
    <ID>NoUnusedImports:NeedsAttentionScreen.kt:com.sketchbook.featureneedsattention.NeedsAttentionScreen.kt</ID>
    <ID>ViewModelForwarding:NeedsAttentionDetailPane.kt:MacImportDetail(target.finding, vm, onDismiss)</ID>
    <ID>ViewModelForwarding:NeedsAttentionDetailPane.kt:MissingSampleDetail(target.finding, vm, onDismiss)</ID>
  </CurrentIssues>
</SmellBaseline>
