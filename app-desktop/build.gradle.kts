// Compose Desktop application — wired in PR-18 (Compose plugin, Metro graph, NavStack root,
// OS integration, packageDmg/packageMsi). For PR-1 this module is an intentional skeleton:
// no plugins, no sources, no dependencies. The placeholder lets `./gradlew projects` list it.

plugins {
    base
}
